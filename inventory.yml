k3s:
  children:
    k3s_servers:
    k3s_clients:

k3s_servers:
  hosts:
    apollo.icestation.is:

k3s_clients:
  hosts:
    aquarius.icestation.is:
    bepicolombo.icestation.is:

nfs:
  hosts:
    calipso.icestation.is:
