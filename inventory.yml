k3s:
  children:
    k3s_servers:
    k3s_clients:

k3s_servers:
  hosts:
    apollo.icestation.is:
      ansible_user: rrstickney
k3s_clients:
  hosts:
    aquarius.icestation.is:
      ansible_user: rrstickney
    bepicolombo.icestation.is:
      ansible_user: rrstickney

nfs:
  hosts:
    calipso.icestation.is:
      ansible_user: redhat
